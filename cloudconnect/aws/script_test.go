package aws

/*
import "testing"

func TestPermissionScript(t *testing.T) {
	err := PermissionScript()
	if err != nil {
		t.Fatalf("could not run permission script. error %s", err)
	}

	t.Log("success")
}
*/
